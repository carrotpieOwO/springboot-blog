<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carrot.blog.repository.PostRepository">

  <select id="findAll" resultType="com.carrot.blog.model.post.Post">
    select * from post order by id desc
  </select>
  
  <insert id="write">
  	insert into post(title, content, userId, createDate) 
  	values(#{title}, #{content}, #{userId}, now())
  </insert>
  
  <select id="findById" resultType="com.carrot.blog.model.post.Post">
    select * from post where id=#{id}
  </select>
  
  <update id="update">
	  update post set title=#{title}, content=#{content} where id=#{id}
  </update>
  
   
   <delete id="delete">
   	delete from post where id=#{id}	
   </delete>
</mapper>